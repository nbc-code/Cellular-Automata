//Done with this
//#include<stdio.h>

/* Initialize array */
//void init1DCA(struct ca_data *ca, int initVal)
//{
    /*For each cell in the array this will iterate through the array and initialize each cell to 0.
    Note: the array in main could be larger than the input of number of cells to initialize.*/

    /* From Part A
    for(int i = 0; i < numCells; i++)
    {
        *arrStart++ = 0;
    }*/
//}

/* A call to this function allows the client to set the specified cell
to a specified state. */
//int set1DCACell(struct ca_data *ca, unsigned int arrIndex, unsigned char setState)
//{
    /*From part A
    if(arrIndex < 0)
    {
        printf("Error: Index is negative"); //Cannot accept negative array index input
    }
    else
    {
    */
        /* We only want to set the cell at the specified index to the specified state */
    /*  arrStart[arrIndex] = setState;
    }*/
//}

/* Display array */

//void display1DCA(struct ca_data *ca)
//{
    //Prints the array out to the desired number of cells
    /* From part A
    for(int i = 0; i < numCells; i++)
    {
        printf("%u ", arrStart[i]);
    }

    printf("\n");
    */

//}
